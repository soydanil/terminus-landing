{
  "name": "terminus-web",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.14.7"
  },
  "devDependencies": {
    "wrangler": "^4.75.0"
  }
}
