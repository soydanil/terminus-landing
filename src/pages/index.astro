---
import "../styles/global.css";

/**
 * La página de Terminus.
 *
 * **Enseña el producto antes de describirlo.** La captura de la aplicación
 * entra en los primeros 600 px y queda recortada por el borde de la pantalla a
 * propósito: quien llega ve la cosa funcionando y decide si le sirve sin leer
 * un párrafo. Es HTML, no una imagen — se lee con lector de pantalla, escala
 * sin pixelarse, y los rótulos están copiados de la app («tarea», «material»,
 * «carpeta de trabajo», «Memoria», «solo lectura»), no inventados para la
 * foto. Una captura que promete una interfaz que no existe se paga en la
 * primera instalación.
 *
 * **La promesa es lo que el producto hace, no lo que no hace.** Esta página
 * estuvo construida entera sobre «el agente no puede escribir tu repo», y esa
 * frase describía un mecanismo interno como si fuera el producto. Hoy Terminus
 * pide permiso de escritura en GitHub y publica ramas y pull requests: lo que
 * lo hace seguro no es que no escriba, es **quién** escribe y con qué delante.
 * El agente trabaja en una carpeta aparte; la publicación la hace la app, con
 * el cambio completo delante de quien lo aprueba. Eso es lo que se cuenta —
 * una landing que promete «nunca escribe» sobre una app que abre pull requests
 * se desmiente sola en la primera pantalla de permiso de GitHub.
 *
 * **Los permisos van con su nombre y con su número.** Contenido, pull requests
 * e issues, más el de metadatos que GitHub añade a todas. Cuatro, no tres:
 * quien cuente los de la pantalla y encuentre uno más deja de creerse el resto
 * de la página.
 *
 * **No promete lo que la app todavía no hace.** Hoy no está firmada con un
 * certificado del sistema, así que Windows avisa en la primera instalación.
 * Se dice aquí —con su sección propia y un enlace desde el botón mismo— en vez
 * de que alguien lo descubra al abrir el instalador: un aviso rojo del sistema
 * operativo que nadie anticipó es lo que hace que la gente cierre y no vuelva.
 *
 * **Y no hay número de versión escrito.** El enlace apunta a la release más
 * reciente, que es la que GitHub resuelve sola. Un número aquí se queda viejo
 * el día que se publique la siguiente, y nadie se acuerda de venir a cambiarlo.
 */
const RELEASES = "https://github.com/soydanil/terminus/releases/latest";
/**
 * **Nombre estable, sin versión dentro.** Escribir
 * `Terminus_0.1.3_x64-setup.exe` aquí rompe el botón el día que salga la
 * siguiente: `latest` resuelve la release, pero el nombre del archivo tiene que
 * existir tal cual dentro de ella. Cada release publica además una copia con
 * este nombre, y así el enlace no caduca.
 */
const DESCARGA = `${RELEASES}/download/Terminus-Windows-Setup.exe`;
/** El icono de la app instalada. Es el isotipo de Danil: la misma marca. */
const ICONO = "/marca/terminus-icono.png";
/**
 * **Nombre estable del instalador de macOS, sin versión dentro.** Igual que el
 * de Windows: `latest` resuelve la release y este nombre tiene que existir tal
 * cual dentro de ella. Cada release publica esta copia, y su corrida se cae si
 * no se puede descargar — el botón no puede quedarse apuntando a un 404 sin que
 * alguien se entere.
 *
 * Es **Apple Silicon**: el git que la app trae dentro se empaqueta desde la
 * máquina que compila, así que un binario universal llevaría un git de una sola
 * arquitectura. Un Mac Intel usaría el git del sistema, camino que la app cubre,
 * pero no recibe actualizaciones automáticas: `latest.json` solo declara arm64,
 * a propósito, para no mandarle un binario que no puede ejecutar.
 */
const DESCARGA_MAC = `${RELEASES}/download/Terminus-macOS.dmg`;
/** El repositorio público de Terminus. */
const REPO = "https://github.com/soydanil/terminus";
/**
 * La versión publicada, preguntada a GitHub **al compilar**.
 *
 * **No se escribe a mano, y esa es toda la decisión.** Un número escrito aquí
 * se queda viejo el día que salga la siguiente release y nadie se acuerda de
 * volver a cambiarlo — la página acaba diciendo una versión que ya no existe,
 * que es peor que no decir ninguna.
 *
 * **Se resuelve al compilar y no en el navegador de quien visita.** Sale HTML
 * estático: se ve con JavaScript apagado, no cuesta una petición a
 * `api.github.com` por visitante —que tiene tope de 60 por hora y por IP— y no
 * hay un hueco que se rellena tarde. A cambio, el número es el de la última
 * compilación: por eso el flujo de despliegue se corre también solo, cada
 * hora, y así una release nueva aparece aquí sin que nadie toque nada.
 *
 * **Falla en silencio y a propósito.** Si GitHub no contesta al compilar, la
 * página sale sin número en vez de romper el despliegue o inventar uno. Un
 * dato que falta se nota menos que un dato falso, y la descarga —que es lo que
 * importa— nunca dependió de esto.
 */
async function versionPublicada(): Promise<string | null> {
  try {
    const r = await fetch(
      "https://api.github.com/repos/soydanil/terminus/releases/latest",
      {
        headers: {
          Accept: "application/vnd.github+json",
          "User-Agent": "terminus-landing",
          // En CI el token viene del propio flujo: sin él, las IP de GitHub
          // Actions comparten el tope por hora y la petición sale rechazada
          // justo los días de más despliegues.
          ...(process.env.GITHUB_TOKEN
            ? { Authorization: `Bearer ${process.env.GITHUB_TOKEN}` }
            : {}),
        },
      },
    );
    if (!r.ok) return null;
    const d = await r.json();
    // `tag_name` viene como `v0.1.14`; la `v` la pone la página.
    return typeof d.tag_name === "string" ? d.tag_name.replace(/^v/, "") : null;
  } catch {
    return null;
  }
}
const VERSION = await versionPublicada();
/**
 * Los cinco agentes que la app sabe lanzar. Van en un dato y no en prosa
 * porque el número importa: «funciona con agentes de terminal» no dice nada;
 * cinco con nombre sí.
 */
const AGENTES = [
  { nombre: "Claude Code", casa: "Anthropic" },
  { nombre: "Codex", casa: "OpenAI" },
  { nombre: "Grok", casa: "xAI" },
  { nombre: "Antigravity", casa: "Google" },
  { nombre: "OpenCode", casa: "Open source" },
];
/**
 * Los permisos que pide la GitHub App, tal como los devuelve la API. Están
 * escritos aquí y no descritos en prosa para que el día que cambien en el
 * registro se vea que esta lista quedó vieja.
 */
const PERMISOS = [
  { que: "contents", nivel: "escritura", para: "Leer el repo y crear ramas" },
  { que: "pull_requests", nivel: "escritura", para: "Abrir la propuesta de cambio" },
  { que: "issues", nivel: "escritura", para: "Comentar y abrir issues" },
  { que: "metadata", nivel: "lectura", para: "Lo que GitHub añade a todas" },
];
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Terminus — agentes de terminal, con barandales</title>
    <meta
      name="description"
      content="Aplicación de escritorio que corre Claude Code, Codex, Grok, Antigravity y OpenCode contra los repositorios de tu organización. Con tu suscripción, en tu máquina. El trabajo sale como artefacto o como pull request que tú apruebas."
    />
    <meta property="og:title" content="Terminus" />
    <meta
      property="og:description"
      content="El agente hace el trabajo. Tú apruebas lo que sale."
    />
    <meta property="og:type" content="website" />
    <meta name="theme-color" content="#0a0d23" />
    <link rel="canonical" href="https://terminus.danil.ai/" />
    <link rel="icon" href={ICONO} type="image/png" />
    <link rel="apple-touch-icon" href={ICONO} />
    <!-- Las fuentes son lo primero que se ve; se precargan para que el titular
         no se dibuje dos veces con dos tipografías distintas. -->
    <link
      rel="preload"
      href="/fuentes/Geist-Variable.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link
      rel="preload"
      href="/fuentes/GeistMono-Variable.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
  </head>

  <body>
    <!-- El fondo. Cuatro capas fijas detrás de todo: rejilla, luz, viñeta y
         grano. Las dos primeras se mueven a distinta velocidad al hacer scroll
         —de ahí la profundidad— y ninguna captura el ratón. -->
    <div class="fondo" aria-hidden="true">
      <div class="fondo__capa fondo__rejilla" data-parallax="0.05"></div>
      <div class="fondo__capa fondo__luz" data-parallax="0.16"></div>
      <div class="fondo__capa fondo__vineta"></div>
      <div class="fondo__capa fondo__grano"></div>
    </div>
    <div class="reglas" aria-hidden="true"><div class="reglas__caja"></div></div>

    <header class="nav">
      <div class="envoltura nav__fila">
        <a class="marca" href="/">
          <span class="marca__placa">
            <img src={ICONO} alt="" width="17" height="17" />
          </span>
          Terminus
        </a>
        <nav class="nav__acciones" aria-label="Principal">
          <a class="nav__enlace" href="#recorrido">Cómo funciona</a>
          <a class="nav__enlace" href="#permisos">Permisos</a>
          <a class="nav__enlace" href="#estado">Estado</a>
          <a class="boton boton--accion boton--chico" href="#instalar">
            Descargar
          </a>
        </nav>
      </div>
    </header>

    <main>
      <!-- ================================================================
           Hero: la promesa, y debajo la cosa funcionando.
           =============================================================== -->
      <section class="envoltura hero">
        <div class="hero__texto">
          <!-- El gesto es literal: se tacha "artificial" y queda
               "inteligencia" sola, parada. No se reemplaza por nada —la idea
               no es que haya una IA mejor, es que a la que ya existe le
               falta esto que sigue, y eso es exactamente lo que cuenta el
               resto de la página (el recorrido, los permisos). -->
          <p class="diff">
            <span class="mono">inteligencia</span>
            <span class="diff__tachado mono">artificial</span>
          </p>
          <p class="pastilla">Aplicación de escritorio · Windows y macOS</p>
          <h1>El agente hace el trabajo. Tú apruebas lo que sale.</h1>
          <p class="hero__bajada">
            Cinco agentes de terminal corriendo dentro de una app, contra los
            repositorios de tu organización, con tu propia suscripción y sin
            que tu código salga de tu máquina. Lo que producen llega como un
            archivo o como un pull request — y no sale hasta que tú lo miras.
          </p>
          <!-- La instalación vive en el hero, no en una sección lejana: el
               botón y el comando lado a lado, mismo peso. El botón es el camino
               de un clic; el comando, el de quien ya tiene Node. Cada uno con
               una nota corta que acota su alcance. -->
          <div class="hero__lanzador">
            <div class="hero__instalar" id="instalar">
              <div class="hero__via">
                <a class="boton boton--accion boton--grande" href={DESCARGA}>
                  <svg
                    class="boton__glifo"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                    aria-hidden="true"
                  >
                    <path
                      d="M3 5.6 10.4 4.5v7.1H3zM11.6 4.3 21 3v8.6h-9.4zM3 12.5h7.4v7.1L3 18.5zM11.6 12.5H21V21l-9.4-1.3z"
                    ></path>
                  </svg>
                  Descargar para Windows
                </a>
                <p class="hero__via-nota">Un clic, sin terminal.</p>
              </div>
              <div class="hero__via">
                <div class="comando">
                  <code>npx @soydanil/terminus</code>
                  <button
                    class="comando__copiar"
                    type="button"
                    data-copiar="npx @soydanil/terminus"
                  >
                    Copiar
                  </button>
                </div>
                <p class="hero__via-nota">Si ya tienes Node.</p>
              </div>
            </div>

            <!-- macOS. El botón está porque el archivo existe: cada release
                 publica su .dmg con este nombre. El día que dejara de existir,
                 lo que hay que quitar es esto — no apagar un interruptor que
                 nadie volvería a mirar. -->
            <a
              class="boton boton--linea boton--grande hero__mac-boton"
              href={DESCARGA_MAC}
            >
              <svg
                class="boton__glifo"
                viewBox="0 0 24 24"
                fill="currentColor"
                aria-hidden="true"
              >
                <path d="M16.4 12.7c0-2.2 1.8-3.3 1.9-3.4-1-1.5-2.6-1.7-3.2-1.7-1.4-.1-2.6.8-3.3.8s-1.7-.8-2.9-.8c-1.5 0-2.9.9-3.6 2.2-1.6 2.7-.4 6.7 1.1 8.9.7 1.1 1.6 2.3 2.7 2.2 1.1 0 1.5-.7 2.8-.7s1.6.7 2.8.7 1.9-1.1 2.6-2.1c.8-1.2 1.2-2.4 1.2-2.4s-2.2-.9-2.4-3.6zM14.3 6.1c.6-.7 1-1.7.9-2.7-.9 0-1.9.6-2.5 1.3-.5.6-1 1.6-.9 2.6 1 .1 1.9-.5 2.5-1.2z" />
              </svg>
              Descargar para macOS
            </a>

            <!-- El aviso de Windows se enuncia aquí, pegado al botón, y no al
                 final de la página. Enterarse después de descargar es la forma
                 más rápida de perder a alguien. -->
            <p class="hero__requisitos">
              {
                VERSION && (
                  <>
                    <a class="version" href={RELEASES}>
                      <span class="version__punto" aria-hidden="true" />
                      <span class="mono">v{VERSION}</span>
                    </a>
                    <span class="punto" aria-hidden="true">
                      ·
                    </span>
                  </>
                )
              }
              <span class="mono">Windows 10 u 11 · 64 bits</span>
              <span class="punto" aria-hidden="true">·</span>
              <span>Se actualiza sola</span>
              <span class="punto" aria-hidden="true">·</span>
              <a class="hero__aviso" href="#estado">
                Windows avisa la primera vez
              </a>
              <span class="punto" aria-hidden="true">·</span>
              <a
                class="hero__aviso"
                href="mailto:hola@danil.ai?subject=Gobernanza%20de%20agentes"
              >
                ¿Agentes contra producción a escala? Hablemos
              </a>
            </p>

            <!-- Los agentes con los que corre, pegados a la instalación — la
                 fila «Works with» de la referencia. Monocromo, los mismos glifos
                 que la sección de compatibilidad de más abajo. -->
            <div class="hero__compat">
              <span class="hero__compat-rotulo">Funciona con</span>
              <div class="hero__agentes">
                <span class="hero__agente">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <path d="M3 18 8 6 13 18M11 18 16 6 21 18" />
                  </svg>
                  Claude Code
                </span>
                <span class="hero__agente">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round" aria-hidden="true">
                    <path d="M12 4a4 4 0 0 1 4 4 4 4 0 0 1 4 4 4 4 0 0 1-4 4 4 4 0 0 1-4 4 4 4 0 0 1-4-4 4 4 0 0 1-4-4 4 4 0 0 1 4-4 4 4 0 0 1 4-4z" />
                  </svg>
                  Codex
                </span>
                <span class="hero__agente">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true">
                    <path d="M5 5 19 19M19 5 5 19" />
                  </svg>
                  Grok
                </span>
                <span class="hero__agente">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <circle cx="12" cy="13" r="7" />
                    <path d="M12 16.5V9M9 12l3-3 3 3" />
                  </svg>
                  Antigravity
                </span>
                <span class="hero__agente">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <rect x="3" y="5" width="18" height="14" rx="2" />
                    <path d="M7 10l3 2-3 2M13 15h4" />
                  </svg>
                  OpenCode
                </span>
              </div>
            </div>
          </div>        </div>

        <!-- La aplicación real: pestañas de tareas arriba —no tres columnas
             siempre fijas— y el panel derecho también por pestañas
             (Adjuntos / Artefactos / Código). La tarea que se ve es la real
             historia de gobernanza: una pregunta de negocio contra material
             en solo lectura, sin ningún repositorio de código conectado —
             por eso no hay pestaña de rama ni de diff en el compositor. -->
        <figure class="marco" aria-label="La ventana de Terminus con una tarea abierta">
          <div class="ventana__barra">
            <span class="marca__placa" aria-hidden="true">
              <img src={ICONO} alt="" width="17" height="17" />
            </span>
            <span class="ventana__titulo">Terminus</span>
            <span class="ventana__controles" aria-hidden="true">
              <span>—</span><span>▢</span><span>✕</span>
            </span>
          </div>

          <!-- Fila de pestañas: a la izquierda las tareas abiertas, a la
               derecha el panel. Es la fila que la captura vieja no tenía —y
               la que más se parece a la app real. -->
          <div class="pestanas-fila">
            <span class="pestanas__icono" aria-hidden="true">
              <svg class="icono" viewBox="0 0 24 24">
                <rect x="3" y="4" width="18" height="16" rx="2" />
                <path d="M9 4v16" />
              </svg>
            </span>
            <div class="pestanas">
              <span class="pestana pestana--activa">
                Cambio de plan a mitad de mes
                <svg class="pestana__x icono" viewBox="0 0 24 24">
                  <path d="M6 6l12 12M18 6 6 18" />
                </svg>
              </span>
              <span class="pestana">Qué ve el cliente al bajar de plan</span>
            </div>
            <div class="pestanas pestanas--panel" aria-hidden="true">
              <span class="pestana pestana--panel">Adjuntos</span>
              <span class="pestana pestana--panel pestana--activa">Artefactos</span>
              <span class="pestana pestana--panel">Código</span>
            </div>
            <span class="pestanas__icono" aria-hidden="true">
              <svg class="icono" viewBox="0 0 24 24">
                <rect x="3" y="4" width="18" height="16" rx="2" />
                <path d="M15 4v16" />
              </svg>
            </span>
          </div>

          <div class="ventana__cuerpo">
            <!-- Barra lateral: tareas agrupadas en proyectos. -->
            <div class="panel panel--izq">
              <div class="boton-app">
                <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M12 4H5a1 1 0 0 0-1 1v14a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1v-7" />
                  <path d="M18.5 2.5a2.1 2.1 0 0 1 3 3L12 15l-4 1 1-4z" />
                </svg>
                Nueva tarea
              </div>

              <p class="panel__rotulo" style="margin-top: 0.4rem;">Proyectos</p>
              <div class="fila">
                <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M4 4h5l2 2h9v11a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1z" />
                </svg>
                <span class="fila__nombre">Facturación</span>
              </div>
              <div class="fila fila--hija fila--activa">
                <span class="fila__nombre">Cambio de plan a mitad de mes</span>
              </div>
              <div class="fila fila--hija">
                <span class="fila__nombre">Qué ve el cliente al bajar de plan</span>
              </div>
              <div class="fila">
                <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M4 4h5l2 2h9v11a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1z" />
                </svg>
                <span class="fila__nombre">Onboarding</span>
              </div>

              <!-- Recientes: sin ícono por fila —solo el de la etiqueta—,
                   igual que en la app real; no son tareas de un proyecto. -->
              <p class="panel__rotulo" style="margin-top: 0.6rem;">Recientes</p>
              <div class="fila fila--sinicono">
                <span class="fila__nombre">Límites del plan Starter</span>
              </div>
              <div class="fila fila--sinicono">
                <span class="fila__nombre">Qué pasa si falla un pago</span>
              </div>

              <div class="fila" style="margin-top: 0.75rem;">
                <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                  <rect x="3" y="5" width="18" height="12" rx="1" />
                  <path d="M2 20h20" />
                </svg>
                <span class="fila__nombre">Esta computadora</span>
                <svg class="icono" viewBox="0 0 24 24" aria-hidden="true" style="margin-left: auto;">
                  <path d="m7 15 5 5 5-5M7 9l5-5 5 5" />
                </svg>
              </div>
            </div>

            <!-- Conversación. Sin avatares: el lado y el fondo ya dicen quién
                 habla, que es lo que decide la app. -->
            <div class="panel panel--centro">
              <div class="franja">
                <span class="franja__titulo">Cambio de plan a mitad de mes</span>
                <span class="franja__dato">$0,04</span>
                <span class="franja__dato">Memoria</span>
                <span class="medidor" aria-hidden="true">
                  <span class="medidor__lleno" style="width: 18%;"></span>
                </span>
                <span class="franja__dato">14k / 200k</span>
              </div>

              <div class="hilo">
                <p class="dicho">
                  Si un cliente cambia de plan a mitad de mes, ¿qué se le cobra?
                  Se lo tengo que explicar hoy y no quiero inventármelo.
                </p>

                <div>
                  <p class="firma">
                    <span class="firma__agente">Claude Code</span>
                    <span class="firma__cuenta">marta@acme.com</span>
                  </p>

                  <div class="linea" style="margin: 0.5rem 0 0.65rem;">
                    <p class="acto">
                      <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                        <path d="M14 2v6h6" />
                      </svg>
                      <span class="acto__verbo">Leyó 14 archivos</span>
                      <span class="acto__sujeto">acme-api/billing</span>
                    </p>
                    <p class="acto">
                      <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                        <circle cx="11" cy="11" r="7" />
                        <path d="m20 20-3.5-3.5" />
                      </svg>
                      <span class="acto__verbo">Buscó 3 veces</span>
                      <span class="acto__sujeto">prorrateo</span>
                    </p>
                    <p class="acto">
                      <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-7" />
                        <path d="M18.5 2.5a2.1 2.1 0 0 1 3 3L13 12l-4 1 1-4z" />
                      </svg>
                      <span class="acto__verbo">Escribió</span>
                      <span class="acto__sujeto">cambio-de-plan.md</span>
                      <span class="acto__marca">+42</span>
                    </p>
                  </div>

                  <p class="respuesta">
                    Se le cobra <strong>la parte proporcional a los días que
                    quedan del mes</strong>, y el cargo aparece en la factura
                    siguiente, no al instante. Si el plan nuevo es más barato no
                    hay devolución: queda como saldo a favor para el mes que
                    viene. Te dejé el detalle y la frase exacta que puedes
                    copiarle al cliente.
                  </p>

                  <p class="entrega" style="margin-top: 0.65rem;">
                    <svg class="icono" viewBox="0 0 24 24" aria-hidden="true">
                      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                      <path d="M14 2v6h6" />
                      <path d="M12 12v6M9 15h6" />
                    </svg>
                    Produjo
                    <span class="entrega__ruta">cambio-de-plan.md</span>
                    <span class="entrega__peso">12,4 kB</span>
                  </p>
                </div>
              </div>

              <div class="compositor">
                <!-- Solo «Contexto», sin fila de «Código»: esta tarea no
                     tiene ningún repositorio conectado para escribir, así
                     que la app no dibuja esa fila. La ausencia es la prueba —
                     no hay rama, porque no hay nada que se pueda escribir. -->
                <p class="compositor__contexto">
                  Contexto:
                  <span class="chip">
                    <svg class="icono" viewBox="0 0 24 24">
                      <path d="M4 4h5l2 2h9v11a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1z" />
                    </svg>
                    acme-api
                  </span>
                  <span class="chip">
                    <svg class="icono" viewBox="0 0 24 24">
                      <path d="M4 4h5l2 2h9v11a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1z" />
                    </svg>
                    docs-producto
                  </span>
                </p>
                <div class="compositor__fila">
                  <span class="tecla-redonda" aria-hidden="true">
                    <svg class="icono" viewBox="0 0 24 24">
                      <path d="M12 5v14M5 12h14" />
                    </svg>
                  </span>
                  <span class="compositor__texto">Pregunta algo</span>
                  <span class="selector">Claude Sonnet ⌄</span>
                  <span class="selector">Razonamiento ⌄</span>
                  <!-- El botón de enviar es el amarillo de acción, sólido —
                       no un contorno. Es el mismo `--accion` del botón de
                       descarga: la app real lo pinta así. -->
                  <span class="tecla-redonda tecla-redonda--enviar" aria-hidden="true">
                    <svg class="icono" viewBox="0 0 24 24">
                      <path d="M12 19V5M5 12l7-7 7 7" />
                    </svg>
                  </span>
                </div>
              </div>

              <p class="consumo">
                <span>Claude</span>
                <span class="medidor" aria-hidden="true">
                  <span class="medidor__lleno" style="width: 78%;"></span>
                </span>
                <span>78 % resta</span>
                <span class="franja-larga">· repone en 2 h 14 min</span>
              </p>
            </div>

            <!-- Artefactos: lo que salió de la tarea, separado de lo que entró.
                 Aquí se ve el contrato del producto — lo adjuntado va marcado
                 `solo lectura`, y lo escrito vive en la carpeta de trabajo. -->
            <div class="panel panel--der">
              <p class="panel__rotulo" style="margin-top: 0.4rem;">
                Producido en esta tarea
              </p>
              <p class="artefacto">
                <span class="sello sello--salida">salida</span>
                <span class="artefacto__ruta">cambio-de-plan.md</span>
                <span class="artefacto__peso">12,4 kB</span>
              </p>

              <p class="panel__rotulo" style="margin-top: 0.6rem;">
                Lo que adjuntaste
              </p>
              <p class="artefacto">
                <span class="sello sello--entrada">entrada</span>
                <span class="artefacto__ruta">acme-api</span>
                <span class="artefacto__peso">solo lectura</span>
              </p>
              <p class="artefacto">
                <span class="sello sello--entrada">entrada</span>
                <span class="artefacto__ruta">docs-producto</span>
                <span class="artefacto__peso">solo lectura</span>
              </p>

              <p class="boton-fantasma">Abrir carpeta de trabajo</p>
            </div>
          </div>
        </figure>      </section>

      <!-- ================================================================
           Qué es. Después de la captura: primero se ve la cosa, luego se dice
           por qué hacía falta.
           =============================================================== -->
      <section class="envoltura seccion" style="margin-top: clamp(3rem, 6vw, 5rem);">
        <div class="encabezado-seccion" style="margin-bottom: 2.5rem;">
          <p class="rotulo">Qué es</p>
          <h2>Los agentes ya están dentro de tu empresa. Casi siempre sin barandales.</h2>
        </div>
        <div class="dos-columnas">
          <p class="suave">
            Alguien de tu equipo ya le está pegando código a un chat, o corriendo
            un agente de terminal sobre el repo de la empresa desde su portátil.
            Lee lo que quiere, escribe lo que quiere, y nadie se entera hasta
            que alguien lo descubre después.
          </p>
          <p class="suave">
            Terminus no viene a prohibir eso: viene a darle una forma. Los mismos
            agentes, la misma potencia, pero con el alcance escrito, el trabajo
            registrado y una persona aprobando lo que sale.
            <strong>El agente propone; tú publicas.</strong>
          </p>
        </div>
      </section>

      <!-- ================================================================
           El recorrido. Cuatro pasos con su dato de máquina debajo — la
           cadena entera de una tarea, de conectar el repo a aprobar el PR.
           =============================================================== -->
      <section class="envoltura seccion" id="recorrido">
        <div class="encabezado-seccion">
          <p class="rotulo">El recorrido</p>
          <h2>De una frase tuya a un cambio revisable.</h2>
          <p class="suave">
            Cuatro pasos. En ninguno el agente decide solo qué sale de la
            máquina.
          </p>
        </div>

        <ol class="recorrido">
          <li class="etapa">
            <span class="etapa__num">01</span>
            <div class="etapa__cuerpo">
              <h3>Eliges a qué repositorios entra</h3>
              <p>
                No «todo tu código privado»: los que marcas, uno por uno, en la
                pantalla de GitHub. La app te lleva ahí y espera a que elijas.
              </p>
              <p class="etapa__dato mono">
                <span class="punto-estado" aria-hidden="true"></span>
                acme/docs-producto
                <span class="etapa__fin">conectado</span>
              </p>
            </div>
          </li>
          <li class="etapa">
            <span class="etapa__num">02</span>
            <div class="etapa__cuerpo">
              <h3>Escribes la tarea en tu idioma</h3>
              <p>
                Sin comandos y sin terminal. La tarea guarda su conversación
                entera, así que se puede retomar mañana donde se quedó.
              </p>
              <p class="etapa__dato">
                «Documenta qué pasa cuando alguien cambia de plan a mitad de
                mes»
              </p>
            </div>
          </li>
          <li class="etapa">
            <span class="etapa__num">03</span>
            <div class="etapa__cuerpo">
              <h3>El agente trabaja en su propia carpeta</h3>
              <p>
                El repositorio entra como material de lectura. El agente no está
                parado dentro de él: corre en una carpeta de trabajo aparte, y
                ahí es donde escribe.
              </p>
              <p class="etapa__dato mono">
                acme/docs-producto
                <span class="etapa__fin">material · solo lectura</span>
              </p>
              <p class="etapa__dato mono">
                sessions/a3f9/work/
                <span class="etapa__fin">aquí escribe</span>
              </p>
            </div>
          </li>
          <li class="etapa">
            <span class="etapa__num">04</span>
            <div class="etapa__cuerpo">
              <h3>Sale como archivo, o como pull request que apruebas</h3>
              <p>
                Puedes quedarte el resultado como archivo y no tocar el repo. O
                publicarlo: la app abre la rama y el pull request con el cambio
                completo delante de ti. <strong>Lo publica la app, no el
                agente</strong>, y no sale hasta que lo apruebas.
              </p>
              <p class="etapa__dato mono">
                <span class="etapa__marca">PR</span>
                docs: cambio de plan a mitad de mes
                <span class="etapa__fin">esperando tu revisión</span>
              </p>
            </div>
          </li>
        </ol>
      </section>

      <!-- ================================================================
           Los permisos, con su nombre y su nivel. La tabla sale de la API de
           GitHub, no de la memoria de nadie: cuatro filas, y la cuarta es la
           que GitHub añade sola.
           =============================================================== -->
      <section class="envoltura seccion" id="permisos">
        <div class="encabezado-seccion">
          <p class="rotulo">Permisos</p>
          <h2>Lo que cambia es dónde, no qué.</h2>
          <p class="suave">
            Terminus pide permiso de escritura, y lo dice aquí igual que lo dice
            la pantalla de GitHub. Lo que acota no es la capacidad — es el
            alcance: <strong>solo los repositorios que marcaste</strong>, y solo
            estos cuatro permisos.
          </p>
        </div>

        <div class="permisos">
          <div class="permisos__cabecera mono">
            <span>Permiso</span><span>Nivel</span><span>Para qué</span>
          </div>
          {
            PERMISOS.map((p) => (
              <div class="permisos__fila">
                <span class="permisos__que mono">{p.que}</span>
                <span class={`permisos__nivel${p.nivel === "lectura" ? " permisos__nivel--lectura" : ""}`}>
                  {p.nivel}
                </span>
                <span class="permisos__para">{p.para}</span>
              </div>
            ))
          }
        </div>
        <p class="nota-fina">
          La conexión es una <strong>GitHub App</strong> de la organización
          <span class="mono">soydanil</span>, no una app registrada en la cuenta
          personal de alguien. Antes esta misma pantalla pedía
          <span class="mono">repo</span> entero —control total sobre todo tu
          código privado— porque por el camino anterior no había forma de pedir
          menos.
        </p>
      </section>

      <!-- ================================================================
           Los cinco agentes. El número es el argumento: cada uno con su casa
           y con la nota de que la suscripción es tuya.
           =============================================================== -->
      <section class="envoltura seccion">
        <div class="encabezado-seccion">
          <p class="rotulo">Los agentes</p>
          <h2>Cinco, con tu suscripción.</h2>
          <p class="suave">
            Corren con tu sesión y tus propios límites de uso. Terminus no
            revende tokens ni guarda tus credenciales de agente: no las
            necesita.
          </p>
        </div>

        <div class="agentes">
          {
            AGENTES.map((a) => (
              <article class="agente">
                <span class="punto-estado" aria-hidden="true" />
                <span class="agente__nombre">{a.nombre}</span>
                <span class="agente__casa mono">{a.casa}</span>
              </article>
            ))
          }
        </div>
        <p class="nota-fina">
          Git y los CLI de los agentes vienen empaquetados dentro de la
          aplicación. No tocan tu sistema y no hay nada más que instalar.
        </p>
      </section>

      <!-- ================================================================
           Momentos. La app en más de una pantalla: conectar una herramienta
           y leer lo que la tarea dejó escrito.
           =============================================================== -->
      <section class="envoltura seccion">
        <div class="encabezado-seccion">
          <p class="rotulo">En la aplicación</p>
          <h2>No es una sola pantalla.</h2>
          <p class="suave">
            Conectar una herramienta y leer lo que el agente escribió son dos
            momentos distintos. Aquí están los dos.
          </p>
        </div>

        <!-- Momento 1 — conectar una herramienta por MCP. -->
        <div class="momento">
          <div class="momento__texto">
            <h3>Conectas una herramienta con un botón.</h3>
            <p>
              Figma, Jira o Confluence entran por OAuth. Cada servidor trabaja
              con los permisos de tu cuenta. Terminus no guarda credenciales de
              nadie.
            </p>
            <p class="dato-linea">
              La conexión es del sistema, no de la conversación.
            </p>
          </div>

          <figure class="vista" aria-label="El panel de herramientas de Terminus, con Figma y Atlassian conectados">
            <div class="ventana__barra">
              <span class="marca__placa" aria-hidden="true">
                <img src={ICONO} alt="" width="17" height="17" />
              </span>
              <span class="ventana__titulo">Herramientas</span>
              <span class="ventana__controles" aria-hidden="true">
                <span>—</span><span>▢</span><span>✕</span>
              </span>
            </div>
            <div class="vista__cuerpo">
              <p class="panel__rotulo">Conectadas</p>
              <p class="vista__fila">
                <svg class="vista__marca" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                  <rect x="8.5" y="3" width="7" height="6" rx="3" />
                  <rect x="8.5" y="9" width="7" height="6" rx="3" />
                  <path d="M8.5 3H5a3 3 0 0 0 0 6h3.5M8.5 9H5a3 3 0 0 0 0 6h3.5" />
                  <circle cx="19" cy="12" r="3" />
                  <path d="M8.5 15v3a3 3 0 1 1-3.5-3z" />
                </svg>
                <span class="vista__nombre">Figma</span>
                <span class="sello sello--salida vista__fin">conectado</span>
              </p>
              <p class="vista__fila">
                <svg class="vista__marca" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round" aria-hidden="true">
                  <path d="M4 11l8 8 8-8" />
                  <path d="M8 5l4 4 4-4" />
                </svg>
                <span class="vista__nombre">Jira</span>
                <span class="vista__sub">y Confluence</span>
                <span class="sello sello--salida vista__fin">conectado</span>
              </p>
              <p class="panel__rotulo" style="margin-top: 0.4rem;">Disponibles</p>
              <p class="vista__fila">
                <svg class="vista__marca" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                  <path d="M12 2a10 10 0 0 0-3.2 19.5c.5.1.7-.2.7-.5v-1.7c-2.8.6-3.4-1.3-3.4-1.3-.5-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 .1 1.5 1 1.5 1 .9 1.5 2.3 1.1 2.9.8.1-.6.3-1.1.6-1.4-2.2-.3-4.5-1.1-4.5-5a3.9 3.9 0 0 1 1-2.7 3.6 3.6 0 0 1 .1-2.6s.8-.3 2.7 1a9.3 9.3 0 0 1 5 0c1.9-1.3 2.7-1 2.7-1 .4 1 .2 2 .1 2.6a3.9 3.9 0 0 1 1 2.7c0 3.9-2.3 4.7-4.5 5 .3.3.6.9.6 1.8v2.7c0 .3.2.6.7.5A10 10 0 0 0 12 2z" />
                </svg>
                <span class="vista__nombre">GitHub</span>
                <span class="boton-fantasma vista__fin" style="margin-top: 0; padding: 0.25rem 0.6rem;">Conectar</span>
              </p>
            </div>
          </figure>
        </div>

        <!-- Momento 2 — el artefacto abierto. Invertido: la vista a la izquierda. -->
        <div class="momento momento--inverso">
          <div class="momento__texto">
            <h3>El resultado es un archivo, no un mensaje.</h3>
            <p>
              Lo que el agente escribe queda como artefacto en la carpeta de
              trabajo. Se abre, se relee y se comparte. No se pierde al cerrar
              la conversación.
            </p>
            <p class="dato-linea">
              Markdown, texto o lo que la tarea produzca.
            </p>
          </div>

          <figure class="vista" aria-label="El artefacto cambio-de-plan.md abierto en Terminus">
            <div class="ventana__barra">
              <span class="marca__placa" aria-hidden="true">
                <img src={ICONO} alt="" width="17" height="17" />
              </span>
              <span class="ventana__titulo mono">cambio-de-plan.md</span>
              <span class="ventana__controles" aria-hidden="true">
                <span>—</span><span>▢</span><span>✕</span>
              </span>
            </div>
            <div class="vista__cuerpo vista__doc">
              <div class="vista__cabecera">
                <span class="sello sello--salida">salida</span>
                <span class="artefacto__ruta">carpeta-de-trabajo/cambio-de-plan.md</span>
                <span class="artefacto__peso">12,4 kB</span>
              </div>
              <h4>Cambio de plan a mitad de mes</h4>
              <p>
                Al cliente se le cobra la parte proporcional a los días que
                quedan del mes. El cargo aparece en la factura siguiente, no al
                instante.
              </p>
              <ul>
                <li><strong>Sube de plan:</strong> se prorratea el mes en curso.</li>
                <li><strong>Baja de plan:</strong> el saldo queda a favor.</li>
                <li>La factura del mes siguiente lleva el ajuste.</li>
              </ul>
            </div>
          </figure>
        </div>      </section>

      <!-- ================================================================
           Herramientas. Entran por MCP con OAuth, con los permisos de la
           cuenta de cada quien — no una cuenta de servicio compartida.
           =============================================================== -->
      <section class="envoltura seccion">
        <div class="encabezado-seccion">
          <p class="rotulo">Herramientas</p>
          <h2>Y tus herramientas, con tus permisos.</h2>
          <p class="suave">
            Entran por MCP con tu propia cuenta, y llegan a los cinco agentes —
            no a dos. El que no puede iniciar sesión en una lo dice antes, no
            después de gastarte el turno.
          </p>
        </div>
        <div class="compat">
          <div class="compat__grupo">
            <div class="logos">
              <span class="logo">
                <svg class="logo__glifo" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                  <rect x="8.5" y="3" width="7" height="6" rx="3" />
                  <rect x="8.5" y="9" width="7" height="6" rx="3" />
                  <path d="M8.5 3H5a3 3 0 0 0 0 6h3.5M8.5 9H5a3 3 0 0 0 0 6h3.5" />
                  <circle cx="19" cy="12" r="3" />
                  <path d="M8.5 15v3a3 3 0 1 1-3.5-3z" />
                </svg>
                <span class="logo__nombre">Figma</span>
              </span>
              <span class="logo">
                <svg class="logo__glifo" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round" aria-hidden="true">
                  <path d="M4 11l8 8 8-8" />
                  <path d="M8 5l4 4 4-4" />
                </svg>
                <span class="logo__nombre">Jira</span>
              </span>
              <span class="logo">
                <svg class="logo__glifo" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" aria-hidden="true">
                  <path d="M3.5 15c4-5.5 8.5 3 17-1.5" />
                  <path d="M20.5 9C16.5 14.5 8 6 3.5 10.5" />
                </svg>
                <span class="logo__nombre">Confluence</span>
              </span>
              <span class="logo">
                <svg class="logo__glifo" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                  <path d="M12 2a10 10 0 0 0-3.2 19.5c.5.1.7-.2.7-.5v-1.7c-2.8.6-3.4-1.3-3.4-1.3-.5-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 .1 1.5 1 1.5 1 .9 1.5 2.3 1.1 2.9.8.1-.6.3-1.1.6-1.4-2.2-.3-4.5-1.1-4.5-5a3.9 3.9 0 0 1 1-2.7 3.6 3.6 0 0 1 .1-2.6s.8-.3 2.7 1a9.3 9.3 0 0 1 5 0c1.9-1.3 2.7-1 2.7-1 .4 1 .2 2 .1 2.6a3.9 3.9 0 0 1 1 2.7c0 3.9-2.3 4.7-4.5 5 .3.3.6.9.6 1.8v2.7c0 .3.2.6.7.5A10 10 0 0 0 12 2z" />
                </svg>
                <span class="logo__nombre">GitHub</span>
              </span>
              <span class="logo">
                <svg class="logo__glifo" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round" aria-hidden="true">
                  <path d="M3 5h18l-2.4 14H5.4z" />
                  <path d="M10 10.5h4l-.7 4h-2.6z" />
                </svg>
                <span class="logo__nombre">Bitbucket</span>
              </span>
            </div>
          </div>
        </div>
      </section>

      <!-- ================================================================
           Idiomas. Va aquí y no en una nota al pie porque es la diferencia
           entre «software para devs» y «software para quien opera el negocio».
           =============================================================== -->
      <section class="envoltura seccion">
        <div class="encabezado-seccion">
          <p class="rotulo">Idioma</p>
          <h2>Habla el idioma de quien la usa.</h2>
          <p class="suave">
            Español e inglés completos —chat, configuración, código, errores— y
            el idioma es del espacio de trabajo, no de la máquina: cambia al
            cambiar de cliente.
          </p>
        </div>

        <div class="lenguas">
          <span class="lengua">
            <span class="lengua__nombre">Español</span>
            <span class="lengua__nota mono">completo</span>
          </span>
          <span class="lengua">
            <span class="lengua__nombre">English</span>
            <span class="lengua__nota mono">completo</span>
          </span>
          <span class="lengua">
            <span class="lengua__nombre">Maayatʼaan</span>
            <span class="lengua__nota mono">paquete instalable</span>
          </span>
        </div>
        <p class="nota-fina">
          El maayatʼaan entra como paquete: se elige en el selector y elegirlo
          lo instala. Y el agente contesta en la lengua del espacio de trabajo,
          no solo la interfaz.
        </p>
      </section>

      <!-- ================================================================
           En qué punto está. Lo que falta, escrito, no escondido.
           =============================================================== -->
      <section class="envoltura seccion" id="estado">
        <div class="encabezado-seccion">
          <p class="rotulo">Estado</p>
          <h2>En qué punto está.</h2>
        </div>

        <div class="estado">
          {
            VERSION && (
              <div class="estado__fila">
                <p class="estado__que">Última versión</p>
                <p class="estado__detalle">
                  <span class="mono">v{VERSION}</span>. La app instalada se
                  actualiza sola; este número sale de la release publicada, no
                  está escrito en la página.
                </p>
                <p class="estado__marca">
                  <a class="estado__enlace" href={RELEASES}>
                    Ver notas
                  </a>
                </p>
              </div>
            )
          }
          <div class="estado__fila">
            <p class="estado__que">Windows 10 u 11</p>
            <p class="estado__detalle">
              Instalador de 64 bits. Se actualiza sola.
            </p>
            <p class="estado__marca">
              <span class="punto-estado" aria-hidden="true"></span>Disponible
            </p>
          </div>
          <div class="estado__fila">
            <p class="estado__que">macOS</p>
            <p class="estado__detalle">
              Apple Silicon. Se actualiza sola. En un Mac Intel corre, pero
              las actualizaciones hay que bajarlas a mano.
            </p>
            <p class="estado__marca">
              <span class="punto-estado" aria-hidden="true"></span>Disponible
            </p>
          </div>
          <div class="estado__fila">
            <p class="estado__que">Firma de código</p>
            <p class="estado__detalle">
              Sin certificado. Windows avisa la primera vez:
              <span class="mono">Más información → Ejecutar de todos modos</span>.
              En macOS, <span class="mono">botón derecho → Abrir</span>.
            </p>
            <p class="estado__marca">
              <span class="punto-estado punto-estado--apagado" aria-hidden="true"></span>
              Pendiente
            </p>
          </div>
          <div class="estado__fila">
            <p class="estado__que">
              <a class="estado__enlace" href={REPO}>Canal de distribución</a>
            </p>
            <p class="estado__detalle">
              Público. Cada versión, con su instalador y su firma. El instalador
              de <span class="mono">npx</span> es Apache-2.0; el código de la app
              todavía no es público.
            </p>
            <p class="estado__marca">
              <span class="punto-estado" aria-hidden="true"></span>
              Público
            </p>
          </div>
        </div>
      </section>

      <!-- ================================================================
           Cierre.
           =============================================================== -->
      <section class="envoltura seccion">
        <div class="cierre">
          <span class="marca__placa marca__placa--grande">
            <img src={ICONO} alt="" width="26" height="26" />
          </span>
          <h2>Instálalo y dale una tarea de verdad.</h2>
          <p class="hero__bajada" style="margin: 0 auto;">
            Trae Git y los CLI de los agentes adentro. Tú pones tu suscripción
            de Claude, Codex o el que uses — nosotros no la vemos ni la
            guardamos. Tu código no sale de tu máquina.
          </p>
          <p class="cierre__recap">
            <span>
              <span class="punto-estado" aria-hidden="true"></span>Los repos que tú marcas
            </span>
            <span>
              <span class="punto-estado" aria-hidden="true"></span>El agente, en su carpeta
            </span>
            <span>
              <span class="punto-estado" aria-hidden="true"></span>El pull request, con tu visto bueno
            </span>
          </p>
          <a class="boton boton--accion" href={DESCARGA}>
            Descargar para Windows
          </a>
          <p class="hero__requisitos">
            <span class="mono">Windows 10 u 11 · 64 bits</span>
            <span class="punto" aria-hidden="true">·</span>
            <a class="hero__aviso" href={DESCARGA_MAC}>También para macOS</a>
            <span class="punto" aria-hidden="true">·</span>
            <a class="hero__aviso" href={RELEASES}>Ver versiones</a>
            <span class="punto" aria-hidden="true">·</span>
            <a
              class="hero__aviso"
              href="mailto:hola@danil.ai?subject=Gobernanza%20de%20agentes"
            >
              ¿Agentes contra producción a escala? Hablemos
            </a>
          </p>
        </div>
      </section>
    </main>


    <footer class="pie">
      <div class="envoltura pie__fila">
        <span class="pie__marca">
          <span>Terminus es de</span>
          <a class="firma-casa" href="https://danil.ai">
            <img src="/marca/danil-wordmark.png" alt="Danil" width="52" height="17" />
          </a>
        </span>
        <span>
          <a href="#instalar">Instalación</a> ·
          <a href={RELEASES}>Versiones</a>
        </span>
      </div>
    </footer>

    <script is:inline>
      /*
       * Dos cosas en un solo oyente de scroll: el paralaje del fondo y la barra
       * de arriba, que se enciende al despegarse. Van juntas porque dos oyentes
       * pintando a la vez es la forma más fácil de que una página vaya a
       * tirones — se lee `scrollY` una vez por cuadro y se escribe después.
       *
       * Y el paralaje se apaga entero con `prefers-reduced-motion`: un fondo
       * que se mueve solo es justo lo que marea a quien pide que no se mueva.
       */
      (() => {
        const quieto = matchMedia("(prefers-reduced-motion: reduce)");
        const capas = Array.from(document.querySelectorAll("[data-parallax]"));
        const barra = document.querySelector(".nav");
        /* Las capas sobresalen un 35 % del alto de la ventana por arriba y por
           abajo; pasado ese tope se vería el canto, así que ahí se detienen. */
        const TOPE = () => window.innerHeight * 0.3;
        let pendiente = false;

        const pintar = () => {
          pendiente = false;
          const y = window.scrollY;
          barra.classList.toggle("pegado", y > 8);
          if (quieto.matches) return;
          const tope = TOPE();
          for (const capa of capas) {
            const d = Math.min(y * Number(capa.dataset.parallax), tope);
            capa.style.transform = "translate3d(0," + -d.toFixed(1) + "px,0)";
          }
        };

        addEventListener(
          "scroll",
          () => {
            if (!pendiente) {
              pendiente = true;
              requestAnimationFrame(pintar);
            }
          },
          { passive: true },
        );
        pintar();
      })();
    </script>

    <script is:inline>
      /* El bloque `npx` es copiable: un clic pone el comando en el
         portapapeles y el botón lo confirma un momento. Si el navegador no da
         acceso al portapapeles, el comando sigue ahí para seleccionarlo a
         mano. */
      (() => {
        const btn = document.querySelector(".comando__copiar");
        if (!btn) return;
        btn.addEventListener("click", async () => {
          try {
            await navigator.clipboard.writeText(btn.dataset.copiar);
            const antes = btn.textContent;
            btn.textContent = "Copiado";
            setTimeout(() => {
              btn.textContent = antes;
            }, 1500);
          } catch {}
        });
      })();
    </script>
  </body>
</html>
